"use client";

/**
 * Re-export shared ThemeProvider from @createconomy/ui.
 * This file exists for backwards compatibility with `@/providers/theme-provider` imports.
 */
export { ThemeProvider, useTheme } from "@createconomy/ui/providers/theme-provider";
